version: 2
jobs:
  build:
    docker:
      - image: ubuntu:18.04
      
    steps:
      - checkout
     
      - run: 
          command: |
            apt-get update | apt-get install make | apt-get install git | apt-get install gcc | apt-get install libncursesw5-dev
