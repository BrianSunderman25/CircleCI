version: 2
jobs:
  build:
    docker:
      - image: ubuntu:18.04
      
    steps:
      - checkout
      - run: echo "hello world"
      - run: apt-get update -y && apt-get install make -y && apt-get install git -y && apt-get install libcursesw5-dev -y && apt-get install libcurl4-openssl-dev -y
